Both variants do not seem to work

However this seems to work:

echo 2-3.1.1 > /sys/bus/usb/drivers/usb/unbind
echo 2-3.1.1 > /sys/bus/usb/drivers/usb/bind

Where 2-3.1.1 for example is one of my devices.
